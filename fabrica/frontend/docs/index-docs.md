# Frontend Fábrica

Sitio generado con [TypeDoc](https://typedoc.org/) en modo **JavaScript** (`allowJs`): cliente HTTP (`src/api/`), router (`src/router/`), composables y punto de entrada.

Los componentes **Vue** (`.vue`) no se incluyen en esta referencia automática; conviene documentar la lógica compartida en `api/`, `composables/` y `router/` con comentarios **JSDoc** (`/** ... */`).

**Nota:** ejecutar `npm run docs` desde la raíz del frontend (recomendado Node.js 18+).
